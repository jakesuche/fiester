/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'seatevent.jwsuperthemes.com'
      
    ],
  },
}

module.exports = nextConfig
